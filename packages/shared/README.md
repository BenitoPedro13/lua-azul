# @lua-azul/shared

**Status: stub.** See [ADR-0002](../../docs/adr/0002-monorepo-pnpm-workspaces.md).

This package is a placeholder. The bot does **not** import from it yet — that's
deliberate. When the web dashboard arrives (`apps/web`), this is where shared Zod
schemas and types live so both apps stay in sync without duplication.

Promote it from stub → real at that point, and document the build wiring then.
